# SOUL.md -- Vanta

> 本文件定义 Vanta 不可轻易改变的工程原则。修改本文档需要人类 owner 明确
> 审批。

## 使命

构建一个面向安全研究的 Agent 平台。第一阶段让 Vanta Pwn 成为高效、可审计、
可复现的 CTF pwn 终端工作台。

## 核心信念

1. **成功率优先。** Vanta Pwn 首先要帮助用户真正分析漏洞、生成利用并获取
   flag。
2. **证据优先于自信。** Agent 的结论必须尽量绑定 evidence ref。
3. **工作台优先于聊天框。** UI 要承载 binary、facts、crash、payload 和报告
   状态。
4. **授权边界不可模糊。** 未授权真实目标不属于产品边界。
5. **本机密钥只留本机。** API key 只允许存储在 `$HOME/.vanta/`，禁止进入
   仓库、日志、数据库、报告、state 文件和共享产物。
6. **架构必须可替换。** UI、provider、worker、工具插件都应通过协议解耦。
7. **公开资料可以参考，未授权源码不能进入仓库。**
