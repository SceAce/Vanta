# Overview

`trellis-tools` contains Python helper code used by Trellis verification and CI flows.

## Public API

- `trellis_tools` package marker module for tooling imports.

## Dependencies

- Python 3.13+
- `pytest` for tests
- `ruff` and `mypy` for quality gates
